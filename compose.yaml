services:
  frontend:
    image: fantasy-helper
    build:
      context: fantasy-helper
      target: development
    ports:
      - 5173:5173
    volumes:
      - ./fantasy-helper:/usr/src/app
      - /usr/src/app/node_modules
    restart: always
    environment:
      - CHOKIDAR_USEPOLLING=true